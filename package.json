{
  "name": "fantadrama",
  "private": true,
  "version": "0.1.0",
  "description": "FantaDrama monorepo for a premium drama fantasy roster game with Telegram auth and live bot notifications.",
  "packageManager": "npm@10.11.0",
  "workspaces": [
    "client",
    "server",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "npm --workspace client run dev",
    "dev:server": "npm --workspace server run dev",
    "build": "npm --workspace client run build && npm --workspace server run prisma:generate && npm --workspace server run build",
    "start": "npm --workspace server run start",
    "lint": "npm run typecheck",
    "typecheck": "npm --workspace client run typecheck && npm --workspace server run typecheck",
    "test": "npm --workspaces --if-present run test",
    "format": "echo \"Format workspaces\""
  },
  "engines": {
    "node": ">=22.12 <23"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.65.0",
    "@typescript-eslint/parser": "^8.65.0",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^4.6.0",
    "typescript": "^5.6.0"
  }
}
